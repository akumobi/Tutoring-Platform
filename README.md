# Tutoring Platform API (Django)

## Setup Instructions

1. Clone repo
2. Create and activate virtual environment:
   ```bash
   python -m venv env
   source capenv/bin/activate  # or capenv\Scripts\activate on Windows
